name: Release

on: [push]

jobs:
  publish:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: make

      - name: Run tests
        run: make test

      - name: Get the version
        id: get_version
        if: ${{ startsWith(github.ref, 'refs/tags/v') }}
        run: echo ::set-output name=VERSION::${GITHUB_REF#refs/tags/}

      - name: Get Version Name
        id: get_version_name
        env:
          VERSION_WITHOUT_V: ${{ steps.get_version.output.version.without-v }}
        run: echo '::set-output name=version_name::${VERSION_WITHOUT_V//./_}'

      - name: Upload binaries to release
        uses: svenstaro/upload-release-action@v2
        if: ${{ startsWith(github.ref, 'refs/tags/v') }}
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: target/release/tradukaten.app
          asset_name: tradukaten-${{ matrix.os }}-${{ steps.get_version.outputs.VERSION }}
          tag: ${{ github.ref }}
